<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.dreamlabs.gdthink.gdthink.dao.TbAdminDAO">
 <select id="getSelect" parameterType="TbAdminVo" resultType="TbAdminVo">
			 SELECT *
		  FROM TB_Admin 
</select>
 <select id="getSelectOne" parameterType="TbAdminVo" resultType="TbAdminVo">
			 SELECT *
		  FROM TB_Admin where ADMIN_ID=#{adminId}
</select>
</mapper>